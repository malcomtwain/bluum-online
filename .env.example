# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Google Cloud Run Video Processor (Optional - for cloud processing)
# Leave empty to use local processing (slower)
# Set this after deploying to Cloud Run: https://your-service-name-xxxxx-uc.a.run.app
NEXT_PUBLIC_VIDEO_PROCESSOR_URL=

# Note: FFmpeg settings are not necessary for Netlify deployments
# as they are only used in local development 